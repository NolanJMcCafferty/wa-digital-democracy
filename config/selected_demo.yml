# Operator-edited config that pins the bill/hearing rendered by
# `wa-dd build-bundle`. Phase 3's `find-candidates` writes a candidates.json
# the operator picks from; the chosen candidate's IDs go here.
#
# Active demo: EHB 1501 "CIC unit owner inquiries" — Senate Housing,
# 2026-02-04. Picked from Phase 3 output: 88 testifiers (16 Pro / 69 Con /
# 3 Other) across 8 organizations.

biennium: "2025-26"
bill_prefix: "HB"
bill_number: 1501
chamber: "Senate"

committee:
  acronym: "HOUS"
  csi_id: "34078"

agenda:
  csi_meeting_family_id: "33828"
  csi_agenda_item_family_id: "169654"
  csi_agenda_item_id: "27885"
  label: "EHB 1501 CIC unit owner inquiries"

tvw:
  event_id: "2026021085"

# Optional manual overrides for transcript segmentation when the automatic
# heuristics fall through (per Blueprint §Step 5, last bullet).
transcript_override:
  start_ms: 0
  end_ms: 0
